# dart_scoring_app

The Flutter app itself — see the [repo root README](../README.md) for
what this app does, setup/permissions, gameplay, and known limitations.

Quick commands from this directory:

```bash
flutter pub get
flutter analyze
flutter test
flutter run          # needs a connected device/emulator, or `-d linux`
```
